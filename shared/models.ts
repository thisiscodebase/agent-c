/** Main Eve chat agent — capable, more expensive. */
export const CHAT_MODEL = "xai/grok-4.5";

/** Lightweight analysis (titles, simple classification) — cheap + fast. */
export const NANO_MODEL = "openai/gpt-5.4-nano";
