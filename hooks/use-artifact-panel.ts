"use client";

/**
 * @deprecated Import from `~/hooks/use-detail-panel` instead.
 * Kept so existing artifact callers keep working.
 */
export {
  ARTIFACT_SEARCH_PARAM,
  useArtifactPanel,
  useDetailPanel,
} from "~/hooks/use-detail-panel";
